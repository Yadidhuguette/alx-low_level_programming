#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @e: the integer that must be computed.
 *
 * Return: The absolute value.
 */
int _abs(int e)
{
	if (e >= 0)
	{
		return (e);
	}
	else
	{
		return (-e);
	}
}
